# T05 인수인계 누락 기록 (T05-C15)

## 누락 1건 — BASE_COMMIT 이 저장소 HEAD와 불일치

**발견자**: AI B (인수인계 문서만 받고 작업 시작한 뒤 스스로 발견)
**작업 차단 여부**: 없음 (코드는 동일해 실행에 영향 없었음)
**발견 시각**: 2026-09-01 10:3x KST

### 내용
`HANDOFF.md`의 `BASE_COMMIT`은 `b6043fa36a0edd7e01a4468f322d1c479a5e2533`인데,
AI B가 clone한 시점의 원격 기본 브랜치 HEAD는 `38854f19dca6969e54aeb1f26686aee559b5108e`였다.

사이의 두 커밋은 모두 문서 커밋이라 `src/lib/brief.ts` 코드는 완전히 동일했다.
- `e87d46c` AI A 인수인계: HANDOFF.md 7항목 추가
- `38854f1` HANDOFF.md 서식 정리

즉 "코드 기준 커밋"으로는 맞고 "저장소 HEAD"로는 어긋난 상태였다.

### 수정 전
```
BASE_COMMIT: b6043fa36a0edd7e01a4468f322d1c479a5e2533
```

### 수정 후
```
BASE_COMMIT: b6043fa36a0edd7e01a4468f322d1c479a5e2533
(위 값은 코드 기준 커밋입니다. 이후 커밋 e87d46c·38854f1은 이 HANDOFF.md 문서만
추가·정리한 것이라 src/lib/brief.ts 는 b6043fa와 동일합니다.
AI B 인계 시점의 저장소 HEAD: 38854f19dca6969e54aeb1f26686aee559b5108e)
```

### 판단
문서 자체를 늦게 고치면 "AI B가 받은 내용"과 달라지므로,
**AI B가 실제로 받은 문서(sha256 `6ad1376c9be346bcf406934afc25a5e40b4d40383f2778099949eff0c5560b79`)는
그대로 두고**, 위 수정은 AI B 작업 종료 후 별도 커밋으로 반영했다.
공개 보고서에는 AI B가 받은 원문과 수정본을 모두 싣는다.

---

## 그 외 누락
**없음.** 실행 명령(`npm ci` → `npm run verify:brief`), 통과 검사 6개,
남은 문제 4개와 실제 출력값, 금지 파일 목록은 새 폴더에서 그대로 재현되었고
AI B가 추가 정보를 요구하지 않았다.
