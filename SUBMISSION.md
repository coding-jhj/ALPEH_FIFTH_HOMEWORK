# 제출문 초안 (T04-C27 · T04-C28)

플랫폼 제출 화면에 그대로 옮겨 적는 용도입니다. `<...>` 부분만 실제 값으로 바꾸세요.
이 파일 자체는 제출물이 아닙니다.

---

## 재현·통과 확인 4가지 (T04-C27)

```
어디로 가나요: <배포한 결과물 URL>

무엇을 하나요(3단계 이내):
1) 상단 정보판에서 값·단위·출처·출처 시각·조회 시각·기준 시간대 여섯 가지를 한 화면에서 확인합니다.
2) 아래 합성 검사 패널에서 「회복 (D1-A → D1-B → TIMEOUT → RECOVER-D2)」 버튼을 누릅니다.
3) 실패 상태에서 나타난 「다시 시도 (T04-RECOVER-D2 재생)」 버튼을 누릅니다.

무엇이 보이면 통과: 정보판에 여섯 요소가 모두 보이고, 일별 기록이 서로 다른 KST 날짜 2건이며
어제 대비 변화값이 두 저장값의 차와 같습니다. 합성 패널은 TIMEOUT 직후 stale / timeout,
행 1건, 마지막 정상값 105를 유지하고, 다시 시도 뒤 fresh / none, 행 2건, 값 120, 어제 대비 15가 됩니다.

안 될 때: 다섯 실패가 서로 다른 문구와 다른 다음 행동을 보여 줍니다.
느린 응답(timeout) / 원천 거절(401·403) / 호출 제한(429) / 오프라인 / 형식 변경(schema).
어느 경우에도 마지막 정상값은 지워지지 않고 「오래된 값」 배지가 붙습니다.
```

## AI와 내 판단 3줄 (T04-C28)

아래는 **초안**입니다. 실제로 본인이 한 판단으로 고쳐 적으세요. 사실과 다르면 안 됩니다.

```
AI에게 맡긴 일: Next.js·Supabase 보일러플레이트 작성, 정규화 스키마 검증 코드,
fixture 9종 자동 대조 스크립트, 실패 다섯 갈래 분기 구현.

내가 직접 판단한 일: 어떤 공개 원천을 쓸지(인증 없는 마인크래프트 서버 접속자 수),
기준 기록 시각을 21시 KST 근처로 고정할지, 무로그인 공개 URL에서 조회 버튼을 열어 둘지,
영수증 봉인 뒤 저장값을 잠글지.

AI 제안을 따르지 않은 일: <있으면 적고, 없으면 "없었습니다"라고 적으세요>
```

## 제출 필드 (T04-C34 · T04-C35)

```
결과물 URL: https://<vercel-앱>.vercel.app
소스 URL  : https://github.com/<owner>/<repo>/commit/<40자리 소문자 hex commit SHA>
```

두 URL 모두 **새 시크릿 창에서 로그인 없이** 열리는지 반드시 직접 확인하세요.
GitHub 저장소가 private이면 C01·C02·C29~C33에서 탈락합니다.
